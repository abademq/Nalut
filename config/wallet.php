<?php

return [

    /*
    | عدد أرقام كرت الشحن (أرقام فقط).
    |
    | الأمان يعتمد على نسبة الكروت الصالحة لكل الاحتمالات:
    |   8 أرقام  = 90 مليون احتمال   ← مع 10,000 كرت صالح: تخمين واحد من كل 9,000 يصيب
    |   12 رقم   = 900 مليار احتمال  ← مع 10,000 كرت صالح: واحد من كل 90 مليون
    | مع حد المحاولات تحت، 12 رقم تخلي التخمين مستحيل عملياً.
    */
    'card_digits' => max(8, min(16, (int) env('RECHARGE_CARD_DIGITS', 12))),

    // محاولات شحن فاشلة مسموحة لكل حساب في الساعة، ولكل IP
    'redeem_max_failures_per_user' => (int) env('REDEEM_MAX_FAILURES_USER', 5),
    'redeem_max_failures_per_ip'   => (int) env('REDEEM_MAX_FAILURES_IP', 20),

];
